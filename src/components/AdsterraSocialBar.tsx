/**
 * Adsterra Social Bar Ad - DISABLED
 * User has ads on their PWA app, not needed on website
 */
const AdsterraSocialBar: React.FC = () => {
  // Ads disabled - user has them on PWA app instead
  return null;
};

export default AdsterraSocialBar;
